# ============================================================
# Clinic POS Platform — Environment Variables
# ============================================================
# Copy this file to `.env` to override defaults.
#   cp .env.example .env
#
# All values below are OPTIONAL — docker-compose.yml has
# sensible defaults baked in. Only uncomment what you need.
# ============================================================

# ── PostgreSQL ──
# POSTGRES_DB=clinicpos
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres
# POSTGRES_PORT=5432

# ── Backend API (.NET) ──
# ASPNETCORE_ENVIRONMENT=Development

# ── JWT ──
# JWT_KEY=ClinicPOS-SuperSecret-Key-2026-Min32Chars!!
# JWT_ISSUER=ClinicPOS
# JWT_AUDIENCE=ClinicPOS

# ── RabbitMQ ──
# RABBITMQ_DEFAULT_USER=guest
# RABBITMQ_DEFAULT_PASS=guest
# RABBITMQ_PORT=5672
# RABBITMQ_MANAGEMENT_PORT=15672

# ── Redis ──
# REDIS_PORT=6379

# ── Frontend (Next.js) ──
# NEXT_PUBLIC_API_URL=http://localhost:5001

# ── Exposed Ports (host) ──
# API_PORT=5001
# FRONTEND_PORT=3000
